#!/usr/bin/env ruby

# DISABLED: Reward claiming has been moved to backend (SettlementService)
# This script is no longer used. Rewards are automatically committed to blockchain
# by the backend during epoch settlement.

puts "⚠️  This script has been disabled."
puts "Reward claiming is now handled by the backend (SettlementService)."
puts "Rewards are automatically committed to blockchain during epoch settlement."
puts ""
puts "To check reward status, use the backend API:"
puts "  GET /rewards/epochs"
puts "  GET /rewards/proof?node=0x...&epoch=123"
exit 0

